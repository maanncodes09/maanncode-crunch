package Exceptitional_Handling;

public class TestBox {

}





/*
 * It is like bonding to the caller that code th I might throw an exception and a late if you call me.
 *
 * public class FileNotFoundExecption{
 * static void checkAge(Int age){
 * if (age < 18 ) {
 * throw new ArithmeticException(s:"Not eligible to vote");
 * } else{
 *       System.out.println(x:"Eligible to vote");
 *   }
 * }
 *
 * public static void main(String[] args){
 * check_Age(18);
 * sout("end
 *
 *
 *
 *
 * EXAMPLE-2
 *
 * public class ArithmeticExample{
 *    public static void readFile() throws IOException {
 *          FileReader fr = new FileReader(filename:"abc.text");
 *          BufferReader br = new BufferdReader(fr);
 *          System.out.println(br.r
 *
 *
 * DATE: 31-10-2025
 *
 *         HOMEWORK
 *
 *
 * the difference between class and interface
 * diff b/w implements and keywords
 * object , lifecycle of object and the use of object
 *
 *
 *
 *Iterable interface in java found within the java.lang package signifies that an object can be iterated
 * The primary  purpose of iterator interface is to enable object to be used with the enhance for each loop.
 * The iterable interface defines the single abstract method.
 *
 * public interface Iterable <T>
 {
 * Iterator <T> iterator();
 * }
 *
 *
 *
 * Efficient Data Structure
 * Pre-define Methods for  storage and retrival and mainipultion
 *
 * Dynamic menory management unlike the fixed size of arrays
 * supports Iteration-- shorting -- searching -- thead safe varients
 *
 *
 * Reusebility: Provided ready to use data structure and  algorithms reusing development time.
 *
 * Performance: Implementations are optimized from various use cases.
 * Interoperability:
 *
 *
 *
 * # ArrayList
 * # LinkedList
 * # HashSet
 * # HashMap
 *
 * --> List: This is nothing but just an Interface. List is an Interface in java.util package,
 *           that represent an ordered collection elements it allows duplicate and index based accessed.
 *
 * --> ArrayList: An arraylist is in java is a resizable , implementation that belongs to java collection framework.
 *                Specifically implementing the      . It provides a dynamic array structure.unlike traditional
 *                array which have fixed size upon creation.
 *
 * 1. Resizable Array
 * 2. Ordered-collection: Array list  maintain the injuction order of  the  elements that means elements are stored
 *                        and retrieve in the same sequence.
 *                --> Allows duplicates and null value.
 *                --> It can store multiple identical elements and also includes null values within a array list.
 *     *> Index-based:
 *                --> Elements in arraylist can be accessed by the index using Zero-based indexing.
 *                --> This allows fast random access to elements.
 *                --> Stores object only.
 *                     * Array list stores object and primitive datatype.
 *                     * If we like to store primitive value those corresponding wrapper class must be used.
 *      *> Not Synchronized:
 *                       -->By default , arraylist is not thread safe, multiple threads accessed and modified an arraylist
 *                           concurrently. It  required external synchronization mechanism  prevents data co
 *
 * *> ArrayList default size is 10 elements.
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 * */